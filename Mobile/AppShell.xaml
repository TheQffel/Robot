<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Mobile"
       x:Class="Mobile.AppShell"
       Title="Robot Controller">

    <FlyoutItem Title="Aplikacja" Icon="home.png">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Sterowanie" Icon="control.png">
        <ShellContent ContentTemplate="{DataTemplate local:ControlPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Czujniki" Icon="sensors.png">
        <ShellContent ContentTemplate="{DataTemplate local:SensorsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Ustawienia" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:SettingsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Dokumentacja" Icon="documents.png">
        <ShellContent ContentTemplate="{DataTemplate local:DocumentationPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Konsola" Icon="console.png">
        <ShellContent ContentTemplate="{DataTemplate local:ConsolePage}" />
    </FlyoutItem>
    
</Shell>